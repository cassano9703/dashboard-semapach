/**
 * @fileoverview Firestore Security Rules for SEMAPACH dashboard data.
 *
 * Core Philosophy:
 * This ruleset restricts all data access (read and write) to authenticated
 * users only. This ensures that the dashboard and its underlying data are

 * private and protected from public access.
 *
 * Data Structure:
 * The database stores daily collection data and district progress in separate top-level collections.
 * - /daily_collections/{dailyCollectionId}
 * - /district_progress/{districtProgressId}
 *
 * Key Security Decisions:
 * - All access is restricted to authenticated users (`allow read, write: if isSignedIn();`).
 * - This provides a strong baseline of security, preventing any unauthorized data access.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Restricts read and write access to authenticated users only.
     * @path /daily_collections/{dailyCollectionId}
     * @allow read: if isSignedIn();
     * @allow write: if isSignedIn();
     * @principle Enforces that only signed-in users can view or modify collection data.
     */
    match /daily_collections/{dailyCollectionId} {
      allow read, write: if isSignedIn();
    }

    /**
     * @description Restricts read and write access to authenticated users only.
     * @path /district_progress/{districtProgressId}
     * @allow read: if isSignedIn();
     * @allow write: if isSignedIn();
     * @principle Enforces that only signed-in users can view or modify district progress data.
     */
    match /district_progress/{districtProgressId} {
      allow read, write: if isSignedIn();
    }

    // Helper function to determine if a user is signed in
    function isSignedIn() {
      return request.auth != null;
    }
  }
}
